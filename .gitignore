# .gitignore
# ---------------------------------------------------
# git-ignore가 실행되지 않을때 다음을 순서대로 진행
# $ git rm -r --cached
# $ git add .
# $ git commit -m "fixed untracked files"
# ---------------------------------------------------

# mac 사용자를 위한 .DS.Store 파일
**/.DS_Store

# scss 변환시 해당 내용 정보처리에 대한 자료
**/*.css.map

# prepros로 생긴 config 파일
**/prepros.config

#node-module 삭제
**/node_modules
node_modules
**/package-lock.json

# .vscode
**/*.vscode

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*